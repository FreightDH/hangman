//! TODO: добавить флаг, показывающий, что слово уже было

const questions = {
  closure: 'A function value that references variables from outside its body.',
  frontend: 'Development of the graphical user interface of a website.',
  hoisting:
    'A mechanism in JavaScript in which variables and function declarations are moved to the top of their scope before the code is executed.',
  pending: 'Promise start condition.',
  promise: 'What the fetch() method returns?',
  capturing: 'A phase when the event goes down to the element.',
  shift: 'A method that removes the first element from an array and returns its value.',
  await: 'Operator that is used to wait for Promise to end.',
  arrow: "A shorter syntax for a function which doesn't have its own this, arguments, super, or new.target.",
  cookie: 'A piece of data that is stored on your computer to be accessed by your browser.',
};

export default questions;
